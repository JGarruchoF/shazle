{
  "name": "shazle",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:front": "npm run dev --prefix frontend",
    "dev:back": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "build-only": "npm run build-only --prefix frontend",
    "deploy": "npm run deploy --prefix backend"
  }
}
